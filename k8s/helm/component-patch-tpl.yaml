apiVersion: v1

values:
  images:
    ces-control: cloudogu/k8s-ces-control:0.4.0

patches:
  values.yaml:
    manager:
      image:
        repository: "{{ registryFrom .images.ces-control }}/{{ repositoryFrom .images.ces-control }}"
        tag: "{{ tagFrom .images.ces-control }}"
